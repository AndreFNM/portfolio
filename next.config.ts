const nextConfig = {
  output: 'export',
  distDir: 'out',
  reactStrictMode: true,

  basePath: '/portfolio',
  assetPrefix: '/portfolio',

  images: {
    unoptimized: true,
  },
};

export default nextConfig;
